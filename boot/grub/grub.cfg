# RetroStick — GRUB2 boot menu
# Kernel and initrd live on RETROBOOT (partition 1).
# Kernel root is RETROROOT (partition 2, SquashFS read-only).
# machine= on the kernel cmdline tells the launcher which RetroArch config to use.

set default=saved
set timeout=5
set timeout_style=menu

# Environment block on the FAT32 RETROBOOT partition for savedefault persistence.
set env_file=(hd0,gpt1)/grub/grubenv
load_env
if [ "${saved_entry}" ]; then
    set default="${saved_entry}"
fi

menuentry "Commodore 64" --id c64 {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=c64
    initrd /boot/initramfs-lts
}

menuentry "NES" --id nes {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=nes
    initrd /boot/initramfs-lts
}

menuentry "SNES" --id snes {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=snes
    initrd /boot/initramfs-lts
}

menuentry "Game Boy / GBC" --id gb {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=gb
    initrd /boot/initramfs-lts
}

menuentry "Game Boy Advance" --id gba {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=gba
    initrd /boot/initramfs-lts
}

menuentry "Amiga" --id amiga {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=amiga
    initrd /boot/initramfs-lts
}

menuentry "DOS" --id dos {
    savedefault
    set root=(hd0,gpt1)
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=squashfs ro quiet machine=dos
    initrd /boot/initramfs-lts
}
