# RetroStick — GRUB2 boot menu
# Kernel and initrd live on RETROBOOT (P2, FAT32).
# Root is RETROROOT (P3, ext4) — found by Alpine's default initramfs via label.
# machine= on the kernel cmdline tells the launcher which RetroArch config to use.
#
# $root is set by the loader before this file runs:
#   EFI path:  embedded grub-early.cfg does search --label RETROBOOT
#   BIOS path: grub-install sets $root to the partition containing /boot/grub

set default=0
set timeout=60
set timeout_style=menu

menuentry "Commodore 64" --id c64 {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=c64
    initrd /boot/initramfs-lts
}

menuentry "NES" --id nes {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=nes
    initrd /boot/initramfs-lts
}

menuentry "SNES" --id snes {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=snes
    initrd /boot/initramfs-lts
}

menuentry "Game Boy / GBC" --id gb {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=gb
    initrd /boot/initramfs-lts
}

menuentry "Game Boy Advance" --id gba {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=gba
    initrd /boot/initramfs-lts
}

menuentry "Amiga" --id amiga {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=amiga
    initrd /boot/initramfs-lts
}

menuentry "DOS" --id dos {
    linux /boot/vmlinuz-lts root=LABEL=RETROROOT rootfstype=ext4 rootwait machine=dos
    initrd /boot/initramfs-lts
}
